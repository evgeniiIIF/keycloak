# Cleanup: Удаление мёртвого и избыточного кода

## Высокий приоритет — чистый мёртвый код

### 1. `SKIP_EXPIRY_CHECK` / `SkipExpiryCheck` — мёртвый декоратор
- `bff/src/decorators/auth.decorator.ts` строки 18-19 — экспортируется, но AuthGuard не читает
- `bff/src/guards/auth.guard.spec.ts` — тест на строке 88-94 ссылается на удалённый код
- **Удалить** экспорты + тест

### 2. `UserInfo` интерфейс — не импортируется
- `bff/src/types/keycloak.d.ts` строки 18-23
- Дублирует инлайн-тип в `session.d.ts`
- **Удалить** `UserInfo`, либо заменить инлайн-тип на `UserInfo`

### 3. `Logger.debug()` — ни одного вызова
- `bff/src/shared/logger.ts` строка 61
- **Удалить** метод

### 4. `session_state` и `iss` в DTO — не читаются
- `bff/src/dto/callback.dto.ts` строки 17-22
- Добавлены когда-то чтобы пропустить validation, но контроллер их не использует
- **Удалить** оба поля

### 5. `@Roles()` декоратор — нигде не применяется
- `bff/src/decorators/auth.decorator.ts` строки 21-25
- Вся инфраструктура ролей в AuthGuard (строки 79-108), `RolesMeta`, `ROLES_KEY`
- Ни один endpoint не использует `@Roles()`
- **Удалить** декоратор, `RolesMeta`, `ROLES_KEY`, и ролевую логику из AuthGuard

## Средний приоритет — упрощение

### 6. `config.isProduction` — дублирование проверки env
- `config.nodeEnv === 'production'` повторяется в 4 файлах
- **Добавить** `isProduction: boolean` в `config/config.ts`, использовать везде

### 7. Дублирование типа `UserInfo`
- `keycloak.d.ts` и `session.d.ts` — одинаковая структура
- **Использовать** `UserInfo` из `keycloak.d.ts` в `session.d.ts`

### 8. `session?.accessToken` проверяется дважды в AuthGuard
- Строка 52 и 81 в `auth.guard.ts`
- При `@RequireSession()` + `@Roles()` вторая проверка冗余на
- **Убрать** проверку на строке 81

## Низкий приоритет — рефакторинг

### 9. `KeycloakClient` в AuthController — инжектится для одного метода
- `revokeRefreshToken` мог бы быть в `AuthService`
- **Перенести** метод в AuthService, убрать зависимость из контроллера

### 10. `JwksService` — обёртка с одним геттером
- Используется только в `backchannel.controller.ts`
- **Инлайнить** или сделать синглтон

### 11. `GET /health` — проверяет ничего
- `this.health.check([])` с пустым массивом
- **Упростить** до `return { status: 'ok' }` без TerminusModule

## Verification
1. BFF компилится без ошибок
2. Все существующие тесты проходят
3. Логин/логаут/work с protected-service работают
