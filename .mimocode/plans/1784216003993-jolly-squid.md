# Plan: Clean up auth module - remove dead code

## Changes

### 1. Delete dead `handleCallback` method from `auth.service.ts`

Remove lines 51-61 — unused method that duplicates `exchangeAndApplyCode`.

### 2. Delete dead tests from `auth.service.spec.ts`

Remove `handleCallback` test block (lines 59-103) and unused `UnauthorizedException` import.

### 3. Make `getLogoutUrl` private in `auth.service.ts`

Change line 154: `getLogoutUrl` → `private getLogoutUrl`

### 4. Make `unregisterSession` private in `auth.service.ts`

Change line 161: `unregisterSession` → `private unregisterSession`

### 5. Fix stale comment in `session.service.ts`

Change line 11: `handleCallback` → `performLogin`

## Files to modify
- `bff/src/auth/services/auth.service.ts`
- `bff/src/auth/specs/auth.service.spec.ts`
- `bff/src/session/services/session.service.ts`
