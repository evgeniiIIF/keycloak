# Audit #4 — Remaining 20 Issues

## auth.controller.ts (15 issues)

### AC-1: `catch (err: any)` → `catch (err: unknown)` (5 sites)
- Lines 66, 81, 98, 143 + `.catch((err: any) =>` at line 98
- Change to `catch (err: unknown)` with `const msg = err instanceof Error ? err.message : 'unknown'`

### AC-2: `@Req() req: any` → `@Req() req: Request` (6 sites)
- Lines 24, 30, 73, 78, 88, 113
- Import `Request` from `express`, use typed request

### AC-3: Callback `err: any` → `err: Error | null` (3 sites)
- Lines 48, 55, 103 — `session.regenerate/save/destroy` callbacks

### AC-4: Null check for `payload.sub`
- Line 141: `payload.sub as string` — add `if (!payload.sub)` guard

## auth.service.ts (2 issues)

### AS-1: Remove `(session as any).id` casts
- Lines 62, 80 — try `session.id` directly; if TS rejects, keep minimal cast

## main.ts (1 issue)

### MT-1: Remove `(req.session as any).csrfToken`
- Line 69 — session.d.ts augmentation should make csrfToken available

## jwks.service.ts (1 issue)

### JW-1: Wrap setInterval callback in try/catch
- Lines 12-14 — prevent unhandled rejection from crashing process

## security.middleware.ts (1 issue)

### SM-1: Replace `(req as any).session` with targeted cast
- Line 57 — use `(req as { session?: SessionWithAuth }).session`

## Files to modify
- `controllers/auth.controller.ts`
- `services/auth.service.ts`
- `main.ts`
- `services/jwks.service.ts`
- `middleware/security.middleware.ts`

## Verification
- `npx nest build` — zero errors
- `npx jest --forceExit` — all tests pass
